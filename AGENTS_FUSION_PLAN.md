# AGENTS.md / CLAUDE.md 融合实施计划

状态：已完成。Batch 1–4 已执行；本文档同时作为本次融合工作的恢复记录。

## 1. 目标

为本项目建立一套跨 Claude Code、Codex 及其他支持 `AGENTS.md` 的编码代理的共享工作协议，核心目标不是让 AI 说更多或规则更多，而是让人类和 AI 各自做最擅长的事，并且让人类始终理解、跟得上、能纠错、能接管。

最终交付：

- 根目录 `AGENTS.md`：跨工具共享的规范正文。
- 根目录 `CLAUDE.md`：Claude Code 兼容入口，复用共享规范并保留本项目现有模块入口。
- `.codex/AGENTS.md`：部署到 `~/.codex` 的 Codex 全局模板，与共享规范保持关键规则一致。
- 必要时更新安装器的检查逻辑，确保新入口不会在部署时丢失。

## 2. 已确认的共同原则

### 人机互补，而不是简单替代

按任务结构分工，不按“某个领域永远属于人类或 AI”分工。

- 人类默认主导：开放世界、目标含糊、需要价值取舍或深层理论理解、需要发现根本性错误、需要把超长材料压缩成少数关键判断，以及复杂多模态感知任务，尤其是细粒度空间关系、跨图一致性、密集图表、复杂截图、长视频片段和现实场景中的异常识别。
- AI 默认主导：规则封闭、目标明确、反馈可验证、需要海量搜索或计算、重复量大、可并行、可逆且能由工具检查的工作；例如围棋一类的封闭规则搜索、代码和日志扫描、精确计算、候选方案生成、机械修改和自动化验证。
- 混合任务：AI 负责扩大搜索范围、整理原始证据、提出候选方案和执行；人类负责目标、意义、取舍、关键感知、纠错和最终验收。
- 以上是默认分工，不是永恒断言。AI 必须根据任务风险、证据质量和当前能力重新校准，并明确不确定性。

### 计划先行，但不过度流程化

非平凡任务必须先“想清楚、理解清楚、计划好”，把计划写成可恢复的共享文档，再进入正式实现。

- 简单、明确、单文件、可逆的小改动：在对话中给出一句话计划即可。
- 跨多个文件、存在歧义、涉及架构/依赖/数据/安全、需要复杂视觉判断或预计长期运行的任务：必须写计划文档。
- 计划文档应先交给人类确认；如果用户明确授权无人值守执行，则写完计划后可在授权范围内继续，但仍须遵守高风险检查点和暂停条件。
- 计划不是形式审批。其目的在于暴露假设、让人类检查方向、记录人机分工，并使下一次会话能够恢复工作。

## 3. 研究依据与取舍

本方案综合了以下资料与本项目现有实践：

- `FerroxLabs/agents-md`：反奉承、禁止编造、遇到歧义先停、最小改动、目标驱动验证、项目学习记录和控制文件长度。
- `agents.md` 开放格式：普通 Markdown 即可；根目录与嵌套目录可分层；离被修改文件最近的规则更具体；不强制固定字段。
- Codex 官方说明：按全局到项目再到当前目录构建指令链；`AGENTS.override.md` 优先；默认合并文档大小有限，因此关键规则必须短而高密度。
- Claude Code 官方最佳实践：先探索，再计划，再编码；为工作提供可执行的验证信号；控制上下文；长时间运行时使用独立复核或明确的停止条件。
- OpenAI Codex、Apache Airflow、Temporal 等大型项目的规则文件：项目事实、命令、边界、生成文件、测试入口和风险约束应写得具体，而不是只写抽象口号。
- 本仓库已有的 `docs/workflow.md`、`docs/safety.md`、`skills/writing-plans`、`skills/executing-plans` 和 `.codex/AGENTS.md`：保留其有效内容，统一冲突的表达，避免另起互相矛盾的工作流。

不直接照搬上游的原因：上游文件面向通用软件仓库；本项目还需要人机协作、复杂视觉任务、中文通俗汇报、长时间 `/goal` 恢复、Claude/Codex 双入口和现有安装器兼容。

## 4. 最终 `AGENTS.md` 的拟定结构

正文会控制在 Codex 默认项目文档上限以内，并尽量保持短、明确、可执行。拟定章节如下：

1. **使命与优先级**：用户明确请求优先；安全与事实优先；规则冲突时说明冲突，不静默选择。
2. **人机分工**：人类占优、AI 占优和混合任务的判断表；复杂视觉/多模态默认保留人类最终判断。
3. **任务分级**：简单任务、普通任务、复杂/高风险/长期任务的计划和确认要求。
4. **理解阶段**：先读相关文件、历史、约束和现状；用通俗语言复述目标、完成标准、非目标、假设和未知点。
5. **计划文档协议**：计划文件的触发条件、固定字段、批准规则、更新规则和恢复方式。
6. **执行协议**：按计划分批实施；每个逻辑阶段前简短说明下一步；不暗中扩大范围；发现计划失效时暂停并更新。
7. **人类检查点**：目标取舍、复杂视觉、多方案选择、高影响变更、安全/隐私/发布/迁移等必须交给人类。
8. **沟通协议**：减少噪声但不隐藏关键过程；区分事实、推断、建议和待决策项；每个有意义的回合用小学生和门外汉也能听懂的语言说明做了什么。
9. **复杂视觉与多模态协议**：优先展示原始证据和直观对比；不以文字摘要替代图像；看不清就说看不清；不能凭文件名或常识脑补。
10. **简单性与复用**：最小可行改动、优先复用、不随意加依赖、不为未来假设增加抽象、不做无关重构。
11. **验证与完成定义**：先定义可验证成功标准；真实运行测试/构建/脚本/截图；报告证据和局限；不能把“看起来对”当成完成。
12. **安全与边界**：秘密、凭证、破坏性 Git 操作、生产资源、外部发布和不可逆操作的确认要求。
13. **长期任务与恢复**：阶段目标、进度、阻塞、停止条件、检查点和下一次会话恢复信息；AI 可以长时间运行，但不能让人类失去可见性。
14. **学习机制**：用户纠正后，把可复用且具体的规则写入项目学习区；定期删除不再有用的规则，防止文件膨胀。
15. **项目事实**：只写已经从本仓库验证过的命令、目录和禁区；未知内容明确标记，不猜测。

## 5. 计划文档的固定模板

对于需要正式计划的任务，AI 默认在仓库根目录创建 `PLAN.md`；已有活动计划时更新它，不另起互相冲突的计划。并行或长期存在多个独立任务时，改用 `plans/<任务名>.md`，避免互相覆盖。

计划至少包含：

- **目标与通俗解释**：要改变什么，以及为什么值得做。
- **完成标准**：哪些可观察结果证明任务完成。
- **当前事实**：已经读到、运行或验证的内容。
- **假设与未知**：哪些仍是推测，怎样验证。
- **范围与非目标**：这次做什么，明确不做什么。
- **人机分工**：AI 执行什么，人类在哪些节点判断或验收。
- **方案与取舍**：候选方案、选择理由和放弃的替代方案。
- **文件与影响面**：预计修改的文件、接口、数据、外部系统和副作用。
- **分步实施**：有依赖顺序，每步都有验证方式。
- **风险、暂停条件与回退**：什么情况必须停下，怎样恢复。
- **人类检查点**：需要看原图、选方案、确认范围或批准高影响操作的具体时刻。
- **进度日志**：已完成、进行中、下一步、阻塞和最后一次验证证据。

计划获批准后，执行者不得无声地改变目标、范围或方案。若新事实使计划不再可靠，必须先说明差异、更新文档并等待确认，除非用户已经明确授权该类调整。

## 6. 人类可跟随的沟通协议

最终规则会把“可跟随性”设为硬性工作要求：

- 开始前：用几句话说清理解、计划和需要人类决定的问题。
- 工具操作前：只说明即将进行的有意义动作，不逐条播报机械命令。
- 阶段完成后：说明完成了什么、证据是什么、下一步是什么；长任务不能连续闷头执行太久。
- 遇到不确定性：明确写出事实、推断、置信度和待确认项。
- 回合结束：先用通俗语言解释“这一轮做了什么、为什么这样做、改了哪些文件、如何验证、还有什么风险”，再补充必要技术细节。
- 不把原始日志、术语和失败尝试整批倾倒给人类；但不能为了简短而隐藏会影响判断的证据。

## 7. 计划中的执行批次

获得批准后按以下批次执行，每批完成后进行真实验证并向人类汇报：

### Batch 1：建立共享正文

- 新增根目录 `AGENTS.md`。
- 将人机分工、计划先行、通俗汇报、复杂视觉和长任务恢复写成可执行条款。
- 保留 FerroxLabs 的反编造、反谄媚、最小改动和验证闭环，但删除与本项目不相符或未经核实的绝对断言。

### Batch 2：建立双入口并保持现有功能

- 更新根目录 `CLAUDE.md`：以 `@./AGENTS.md` 载入共享正文，同时保留现有 `docs/environment.md`、`docs/workflow.md`、`docs/tools.md`、`docs/safety.md` 模块入口。
- 更新 `.codex/AGENTS.md`：使部署到 `~/.codex` 后仍包含同一套核心协作协议；不依赖部署后不存在的仓库相对路径。
- 不改动现有 skills、agents、commands、hooks 的行为，除非验证发现入口冲突。

### Batch 3：部署与验证保护

- 检查 Windows/macOS 安装器是否会复制根目录 `AGENTS.md`，以及 Codex 模板是否仍正确部署。
- 如有必要，只增加直接相关的存在性、大小、入口和内容一致性检查。
- 不触碰凭证、认证文件、运行时状态或用户机器上的全局文件。

### Batch 4：审阅与收尾

- 对最终 diff 做独立的规则审阅：寻找矛盾、过长、不可执行、会造成无限询问或鼓励过度工程化的条款。
- 运行 Markdown/引用/字节数/编码检查、`git diff --check` 和可执行的本地静态验证。
- 用普通语言向人类交付：改了什么、为什么、怎样验证、哪些地方仍需要人类判断。

## 8. 验收标准

完成后必须满足：

- 根目录存在非空 `AGENTS.md`，且关键规则不会因默认文档大小限制被截断。
- 根目录 `CLAUDE.md` 能加载 `AGENTS.md`，原有四个模块引用仍有效。
- `.codex/AGENTS.md` 在脱离仓库、部署到 `~/.codex` 后仍是自洽的，不引用不存在的相对文件。
- 两个入口的核心条款一致：人机分工、计划先行、检查点、通俗汇报、视觉证据、验证、安全和长期任务恢复不能出现冲突。
- 计划规则明确区分简单任务与复杂任务，不要求每个一行改动都创建文档，也不允许复杂任务跳过理解和计划。
- 计划文档包含完成标准、验证命令/方式、风险、回退和人类检查点，而不是只有待办事项。
- 规则要求真实验证并诚实报告，没有测试就不声称测试通过。
- 规则没有把复杂视觉判断绝对化为“所有视觉都由人类做”，而是要求按任务结构和证据质量分工。
- 不新增生产依赖，不修改秘密或运行时凭证，不引入无必要的脚本和目录。
- 工作树中的改动全部能追溯到本次请求；不自动提交 Git commit。

## 9. 风险与应对

| 风险 | 应对 |
| --- | --- |
| `AGENTS.md` 与 `.codex/AGENTS.md` 长期漂移 | 保持核心章节结构一致；加入内容/章节一致性检查；修改任一份时同时审阅另一份。 |
| `CLAUDE.md` 既是兼容入口又是现有全局配置 | 使用导入式入口，不删除现有模块；验证所有被引用文件存在。 |
| 规则过长导致模型忽略重点 | 核心规则短句化；把项目事实留给本地模块；控制根文件大小；定期删掉模型已稳定遵守的冗余规则。 |
| “先确认”使无人值守 `/goal` 永远停住 | 区分交互模式和用户明确授权的无人值守模式；高风险与方向变化仍必须暂停。 |
| 复杂视觉条款被误解为人类承担所有视觉工作 | 明确 AI 可做预处理、裁剪、标注、比较和候选生成；人类只接管最关键的感知与判断。 |
| 过度汇报反而增加噪声 | 只在有意义阶段汇报；用通俗摘要加证据索引，而不是逐条倾倒日志。 |
| 上游规则随时间变化 | 记录调研日期和来源类别；把可迁移原则写入本项目，不把外部仓库当运行时依赖。 |

## 10. 回退方案

本次不创建 Git commit。若人类审阅后不接受方案，删除新增计划文档即可，最终规则文件不会被改动。执行阶段若某一批次验证失败：

1. 停止继续修改；
2. 报告失败命令、实际输出、受影响文件和当前假设；
3. 由人类选择修正、调整计划或回退；
4. 不使用 `git reset --hard`、强制覆盖用户文件或其他不可逆操作作为默认回退手段。

## 11. 已确认的设计决策

- 采用“根目录 `AGENTS.md` 为共享正文、根目录 `CLAUDE.md` 为导入入口、`.codex/AGENTS.md` 为部署后自洽副本”的三层结构。
- 正式复杂任务默认使用 `PLAN.md`；并行任务可以使用 `plans/<任务名>.md`，避免计划互相覆盖。
- 交互任务默认等待计划确认；明确授权的无人值守任务可以在写完计划后继续低风险工作，但高风险操作和方向变化仍必须暂停。
- 最终入口文件、兼容入口、Codex 模板、安装器保护和文档说明均已生成并完成静态验收。

## 12. 执行记录

- Batch 1：新增根目录 `AGENTS.md`，写入人机分工、计划先行、复杂视觉、人类决策包、通俗汇报、验证闭环和长期任务协议。
- Batch 2：更新根目录 `CLAUDE.md` 为共享规则导入入口；更新 `.codex/AGENTS.md` 为脱离仓库后仍自洽的全局模板。
- Batch 3：Windows/macOS 安装器继续复制新入口，排除本计划文档，并新增 `AGENTS.md` 安装自检。
- Batch 4：更新 `README.md` 和 `docs/safety.md`，完成 `git diff --check`、临时 Node 规则断言、PowerShell 语法解析和入口/篇幅检查；临时验证文件已删除。
- 未运行真实安装、完整 Claude Code/Codex 会话或 macOS `bash -n`：当前环境是 Windows，且这些操作会触及用户级安装或外部运行时；相关限制已在最终交付中注明。
